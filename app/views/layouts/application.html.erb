<!DOCTYPE html>
<html>
<head>
  <title>A Story Or Two</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
  <link href='http://fonts.googleapis.com/css?family=Rancho' rel='stylesheet' type='text/css'>
  <link rel="shortcut icon" href="favicon.png" />
</head>
<body>
<div id="wrap">
  <a href="/"><h1 class="header">A Story or Two</h1></a>
    <div class="user_links">
    <% if session[:login_id].present? %>
      <p class="wood_text">Hello <%= @user.name %>!</p>
		<%= link_to "My Profile", "/users/#{@user.id}" %>
      <%= link_to "Log Out", logout_url, :method => "delete" %>
    <% else %>
      <%= link_to "Sign Up", new_user_url  %>
      <%= link_to "Login", new_login_url %>
    <% end %>
    </div><!-- .user_links-->
<%= yield %>
</div><!-- #wrap -->
</body>
</html>
